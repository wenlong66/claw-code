# Rust 使用指南

这份指南是 [`../USAGE_cn.md`](../USAGE_cn.md) 的 Rust 工作区补充说明，覆盖 `claw` CLI 二进制。如果你刚开始接触，请把 doctor 健康检查作为第一次运行：先启动 `claw`，然后运行 `/doctor`。

## 要点

请优先使用上层的任务型指南，那里包含：

- 工作区构建与测试命令
- 认证设置
- 交互式与一次性 `claw` 示例
- session 恢复流程
- mock parity harness 命令
